<turbo-stream action="replace" target="toggle-favourite-<%= @cinema.id %>">
  <template>
    <%= render partial: 'favourites/toggle_button', locals: { cinema: @cinema }, formats: [:html] %>
  </template>
</turbo-stream>
